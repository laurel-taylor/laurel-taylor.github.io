import './WordGrid.css'

function WordGrid({ guesses }) {
    return (
        <div className="word-grid">
            {/* TODO render the word grid */}
            WordGrid
        </div>
    )
}

export default WordGrid
